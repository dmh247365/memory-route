const APIFeatures = require('../utils/apiFeatures');

describe.skip('APIFeatures', () => {
  it('filter function', () => {
    // They key questions to take in
    // 1 - what does the function take in
    // 2 - what does the function do
    // 3 - what does the function return
    // ** aside - is the function a async fn?
    // Analysing the function
    // 1 -
    // Arrange
    // Act
    // Assert
    // expect(typeof viewsController.getHome).toBe('function');
    // expect(res.statusCode).toBe(200);
    // expect(res._getRenderView()).toBe(renderedView);
    // expect(res._getRenderData());
  });
});
